#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    copy_elem = my_list[:]

    if idx < 0 or idx >= len(copy_elem):
            return copy_elem
        copy_elem = element
        return copy_elem
